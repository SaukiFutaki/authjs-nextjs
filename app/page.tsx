// import { AppleCardsCarouselDemo, DummyContent } from "@/components/demo/apple-demo";
// import { GetAllEvents,GetActiveEvents, GetInactiveEvents } from "@/lib/actions/get-api";
// import { EventData } from "@/types";

export default async function Home() {
  // const data = await GetInactiveEvents();

  // Map API data to the format needed for the carousel cards
  // const cardsData = data.listEvents.map((event: EventData) => ({
  //   category: event.category,
  //   title: event.name,
  //   src: event.mediaCover,
  //   content: (
  //     <DummyContent
  //       description={event.summary}
  //       imageUrl={event.imageLogo}
  //       link={event.link}
  //     />
  //   ),
  // }));

  return (
    <div className="">
     
    </div>
  );
}